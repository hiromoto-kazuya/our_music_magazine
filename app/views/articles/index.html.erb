<h1>Top page</h1>
<table>
  <tr>
    <th>タイトル</th>
    <th>内容</th>
  </tr>

<% @articles.each do |article| %>
  <tr>
    <td><%= article.title %></td>
    <td><%= article.content %></td>
    <td><%= link_to '詳細を確認する', article_path(article.id) %></td>
  </tr>
<% end %>
</table>

<%= link_to '新しく記事を投稿する', new_article_path %>
