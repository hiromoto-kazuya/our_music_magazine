<section id="user-detail" class="scroll-page bg-white">
  <div class="page-header">
    <h1>ユーザープロフィール</h1>
  </div>
  <table>
    <tr>
      <th>お名前</th>
      <td><%= @user.name %></td>
    </tr>
    <tr>
      <th>メールアドレス</th>
      <td><%= @user.email %></td>
    </tr>
  </table>
</section>
<%= link_to 'お気に入り一覧', favorites_path(id: @user.id) %>
