var elements = document.querySelectorAll('[data-article_id="<%= article.id %>"]')

elements.forEach(function(element) {
  element.innerHTML = ("<%= j(render 'favorites/favorite',{favorite: favorite, article: article})%>");
})
